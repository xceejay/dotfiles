git status
message="added changes"
git add .
read -p "Enter your commit message💬:" message
git commit -m "$message"
git push origin && notify-send -t 300 "succesfully pushed changes to origin📌"
